在 Flask 中构建**多应用（Multi-App）项目**（例如：用户系统、后台管理、API 服务、商家端等），合理的项目结构对可维护性、可扩展性和团队协作至关重要。

以下是 **推荐的多 App Flask 项目结构**，适用于中大型项目，支持模块化、配置分离、测试友好，并兼容前后端分离架构。

---

## 🏗️ 推荐项目结构（基于 Application Factory + Blueprints）

```
myproject/                          # 项目根目录
│
├── backend/                        # 后端代码（Flask）
│   ├── app/                        # Flask 应用核心包
│   │   ├── __init__.py             # 创建 app 工厂函数
│   │   ├── config.py               # 配置类（开发/测试/生产）
│   │   │
│   │   ├── models/                 # 数据库模型（SQLAlchemy）
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   └── product.py
│   │   │
│   │   ├── api/                    # API 蓝图（前后端分离用）
│   │   │   ├── __init__.py
│   │   │   ├── v1/                 # API 版本控制（可选）
│   │   │   │   ├── __init__.py
│   │   │   │   ├── auth.py         # /api/v1/auth
│   │   │   │   └── users.py
│   │   │   └── v2/                 # 未来升级
│   │   │
│   │   ├── main/                   # 主站（传统渲染页面，可选）
│   │   │   ├── __init__.py
│   │   │   ├── views.py            # Jinja2 模板路由（如博客、官网）
│   │   │   └── templates/
│   │   │
│   │   ├── admin/                  # 后台管理系统蓝图
│   │   │   ├── __init__.py
│   │   │   ├── views.py            # /admin/dashboard
│   │   │   └── templates/admin/
│   │   │
│   │   ├── seller/                 # 商家端蓝图
│   │   │   ├── __init__.py
│   │   │   └── views.py
│   │   │
│   │   ├── utils/                  # 工具函数
│   │   │   ├── jwt_helper.py
│   │   │   └── validators.py
│   │   │
│   │   └── extensions.py           # 初始化扩展（db, cors, migrate 等）
│   │
│   ├── migrations/                 # Flask-Migrate 自动生成
│   ├── tests/                      # 单元测试
│   │   ├── conftest.py
│   │   ├── test_auth.py
│   │   └── test_users.py
│   │
│   ├── requirements.txt            # 依赖
│   ├── run.py                      # 启动脚本（或使用 flask run）
│   └── .env                        # 环境变量（gitignore）
│
├── frontend/                       # 前端项目（可多个）
│   ├── web/                        # 用户端（Vue/React）
│   ├── admin-dashboard/            # 后台管理前端
│   └── seller-portal/              # 商家端前端
│
├── docs/                           # 文档（API 文档、设计稿）
├── docker-compose.yml              # 容器编排（可选）
├── Dockerfile
└── README.md
```

---

## 🔧 关键文件说明

### 1. `app/__init__.py` —— Application Factory
```python
from flask import Flask
from app.extensions import db, migrate, cors
from app.config import config_by_name

def create_app(config_name='development'):
    app = Flask(__name__)
    app.config.from_object(config_by_name[config_name])

    # 初始化扩展
    db.init_app(app)
    migrate.init_app(app, db)
    cors.init_app(app, resources={r"/api/*": {"origins": "*"}})

    # 注册蓝图
    from app.api.v1 import bp as api_v1_bp
    from app.admin import bp as admin_bp
    from app.seller import bp as seller_bp

    app.register_blueprint(api_v1_bp, url_prefix='/api/v1')
    app.register_blueprint(admin_bp, url_prefix='/admin')
    app.register_blueprint(seller_bp, url_prefix='/seller')

    return app
```

### 2. `app/extensions.py` —— 统一管理扩展
```python
from flask_sqlalchemy import SQLAlchemy
from flask_migrate import Migrate
from flask_cors import CORS

db = SQLAlchemy()
migrate = Migrate()
cors = CORS()
```

### 3. `app/config.py` —— 多环境配置
```python
import os

class Config:
    SECRET_KEY = os.getenv('SECRET_KEY', 'dev-secret')
    SQLALCHEMY_TRACK_MODIFICATIONS = False

class DevelopmentConfig(Config):
    DEBUG = True
    SQLALCHEMY_DATABASE_URI = os.getenv('DEV_DATABASE_URL', 'sqlite:///dev.db')

class ProductionConfig(Config):
    DEBUG = False
    SQLALCHEMY_DATABASE_URI = os.getenv('DATABASE_URL')

config_by_name = {
    'development': DevelopmentConfig,
    'production': ProductionConfig,
}
```

### 4. 蓝图示例：`app/api/v1/auth.py`
```python
from flask import Blueprint, request, jsonify
from app.models.user import User
from app.utils.jwt_helper import generate_token

bp = Blueprint('auth', __name__, url_prefix='/auth')

@bp.route('/login', methods=['POST'])
def login():
    data = request.get_json()
    user = User.query.filter_by(email=data['email']).first()
    if user and user.check_password(data['password']):
        token = generate_token(user.id)
        return jsonify(token=token)
    return jsonify(error="Invalid credentials"), 401
```

---

## ✅ 多 App 架构优势

| 优势 | 说明 |
|------|------|
| **高内聚低耦合** | 每个子系统（admin/seller/api）独立开发、测试 |
| **权限隔离清晰** | `/admin` 和 `/seller` 可分别做登录校验 |
| **技术栈灵活** | `main/` 可用 Jinja2 渲染官网，`api/` 专供前后端分离 |
| **易于部署** | 可将不同蓝图部署到不同域名或路径（Nginx 路由） |
| **团队协作友好** | 前端分三个项目，后端按模块分工 |

---

## 🚀 部署建议（生产环境）

- **单进程部署**：所有蓝图在一个 Flask 实例中运行（最常见）
  ```nginx
  # Nginx 配置示例
  location /api/ { proxy_pass http://flask_app; }
  location /admin/ { proxy_pass http://flask_app; }
  location /seller/ { proxy_pass http://flask_app; }
  ```

- **微服务拆分**（高级）：将 `admin`、`seller` 拆成独立 Flask 应用（需服务发现、API 网关）

> 💡 对于大多数项目，**单应用 + 多蓝图** 是最佳平衡点。

---

## 📌 最佳实践

1. **每个功能模块一个 Blueprint**
2. **API 路由统一前缀**：`/api/v1/...`
3. **避免跨模块直接 import**：通过 service 层或公共 utils 解耦
4. **使用 `.env` 管理环境变量**
5. **为每个 Blueprint 写单元测试**

---

## 总结

> **多 App ≠ 多个 Flask 实例**，而是通过 **Blueprints + Application Factory** 实现逻辑上的“多应用”在一个 Flask 项目中。

这种结构：
- 适合从小型项目平滑演进到大型系统
- 兼容前后端分离与传统模板渲染
- 是 Flask 官方推荐的可扩展架构

如果你的项目包含 **用户端、管理后台、商家系统、开放 API** 等多个子系统，强烈推荐采用上述结构！