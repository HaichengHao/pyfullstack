from ensurepip import bootstrapfrom twisted.names.root import bootstrap

## 用PIL自制验证码 
### 使用pil来进行验证码的生成 
```python
"""
@File    :captchagen.py
@Editor  : 百年
@Date    :2025/11/10 12:19
"""
from PIL import Image, ImageDraw, ImageFont
import random
import string


def randcolor():
    return (random.randint(0, 255), random.randint(0, 255), random.randint(0, 255))


def generate_captcha(length, exclude_similar=True):  # 生成指定长度的验证码

    # 先组好验证码
    base_chars = string.ascii_letters + string.digits
    if exclude_similar:
        similar_chars = {'0', 'O', '1', 'l', 'I', 'o'}
        base_chars = [c for c in base_chars if c not in similar_chars]
    code_parts = [random.choice(base_chars) for _ in range(length)]
    final_code = ''.join(code_parts)
    print(final_code)
    # 组成验证码结束
    width, height = 130, 50
    img = Image.new("RGB", (width, height), randcolor())
    img_draw = ImageDraw.Draw(img)
    try:
        font = ImageFont.truetype('arial.ttf', 20)  # 指定字体像素为20
    except IOError:
        font = ImageFont.load_default()

    # tips:计算每个字符的位置,均匀分布+随机微调
    char_width = width // length  # 每个字符大致占的宽度
    for i, char in enumerate(final_code):
        x = i * char_width + random.randint(-1, 5)
        y = random.randint(5, 15)
        img_draw.text((x, y), char, fill=randcolor(), font=font)
    # img.show()
    # 可选：添加干扰线或点（增强安全性）
    for _ in range(3):
        x1, y1 = random.randint(0, width), random.randint(0, height)
        x2, y2 = random.randint(0, width), random.randint(0, height)
        img_draw.line((x1, y1, x2, y2), fill=randcolor(), width=1)

    for _ in range(100):
        x, y = random.randint(0, width), random.randint(0, height)
        img_draw.point((x, y), fill=randcolor())
    img.show()


if __name__ == '__main__':
    generate_captcha(6)

```

### 然后在view中对其进行调用  
```python
@user_bp.route('/captcha', endpoint='captcha', methods=['GET', 'POST'])
def captcha_rt():
    vc_code,captcha = generate_captcha(length=6)

    #保存到redis或session中(最好放到redis中,因为它可以设置失效时间且更安全)
    session['vc_code'] = vc_code


    #将验证码转为二进制
    buffer = BytesIO() #构建缓冲区
    captcha.save(buffer,"JPEG") #将验证码存入缓冲区当中
    buffer_bytes = buffer.getvalue() #读出内容
    resp = make_response(buffer_bytes)
    resp.headers['Content-Type'] = 'image/jpeg' #important:必须设置!!否则会格式不被识别将会出错
    return resp
```
> 值得注意的是关于缓冲区的写法 
> 需要先创建buffer,引入BytesIO  
> 然后调用图片file_storage类型中的save()方法将其二进制存入缓冲区中,并指定格式为jpeg等这样的图片格式  
> 之后从缓冲区中读出内容,用getvalue()将其从缓冲区中取出  
> 注意还需要将其指定响应头中的`Content_Type`为图片的格式    

### html中这样写 
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>登录</title>
    <style>
        .info_login {
            display: block;
            color: rebeccapurple;
        {#float: left;#}

        }
    </style>
</head>
<script src="https://www.tenpay.com/v4/static/js/jquery-1.9.1.min.js">

</script>
<body>
<div>
    <form action="{{ url_for('user.login') }}" enctype="multipart/form-data" method="post" id="lgin">
        {{ login_form.csrf_token }}
       ............
        <p>
            {{ login_form.captcha.label }}:
            <span>
                {{ login_form.captcha }}
                {% if login_form.captcha.errors %}
                    {{ login_form.captcha.errors.0 }}
                {% endif %}
            </span>
            <img src="{{ url_for('user.captcha') }}" alt="炒鸡验证码" title="看不清？点击刷新验证码" id="cap">
        </p>


        <button onclick="document.getElementById('lgin').click()">登录</button>
        {#        <button   onclick="document.getElementById('lgin').onsubmit()">登录</button>#}
        <input id="lgin" type="submit" hidden="hidden">
    </form>
</div>
<script>
    $(document).ready(
        function () {
            $('#cap').click(
                function () {
                    console.log('---------')
                    $(this).attr('src', "{{ url_for('user.captcha') }}?ran=" + Math.random()); /*这样写是因为服务机制，如果请求路由不变就不发生变化*/
                }
            );
        }
    )
</script>
</body>
</html>
```

### 测试bootsrap与flask-wtform的结合 
注意，记得安装  
```commandline
pip install flask-bootstrap
```   
然后进行初始化  
```python
from flask_bootstrap import Bootstrap
bootstrap = Bootstrap()
bootstrap.init_app(app)
```

或者不用那么麻烦,只在html中这样写 
```html
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
```

### 其实flask_bootstrap已经淘汰！！！  
所以还是用之前那样写就可以  
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>用户注册</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .captcha-img {
            cursor: pointer;
            height: 38px; /* 与 input 高度对齐 */
            margin-left: 10px;
            border: 1px solid #ced4da;
            border-radius: 0.375rem;
            padding: 2px 6px;
        }
        .error-message {
            color: #dc3545;
            font-size: 0.875em;
            margin-top: 0.25rem;
        }
    </style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5">
                <div class="card shadow-sm">
                    <div class="card-header text-center bg-white">
                        <h4 class="mb-0 text-primary">會員註冊</h4>
                    </div>
                    <div class="card-body">
                        <form action="{{ url_for('user.signup') }}" method="post" enctype="multipart/form-data">
                            {{ signup_form.csrf_token }}

                            <!-- 姓名 -->
                            <div class="mb-3">
                                {{ signup_form.name.label(class="form-label") }}
                                {{ signup_form.name(class="form-control") }}
                                {% if signup_form.name.errors %}
                                    <div class="error-message">{{ signup_form.name.errors[0] }}</div>
                                {% endif %}
                            </div>

                            <!-- 密码 -->
                            <div class="mb-3">
                                {{ signup_form.pwd.label(class="form-label") }}
                                {{ signup_form.pwd(class="form-control") }}
                                {% if signup_form.pwd.errors %}
                                    <div class="error-message">{{ signup_form.pwd.errors[0] }}</div>
                                {% endif %}
                            </div>

                            <!-- 确认密码 -->
                            <div class="mb-3">
                                {{ signup_form.repwd.label(class="form-label") }}
                                {{ signup_form.repwd(class="form-control") }}
                                {% if signup_form.repwd.errors %}
                                    <div class="error-message">{{ signup_form.repwd.errors[0] }}</div>
                                {% endif %}
                            </div>

                            <!-- 头像上传 -->
                            <div class="mb-3">
                                {{ signup_form.icon.label(class="form-label") }}
                                {{ signup_form.icon(class="form-control") }}
                                {% if signup_form.icon.errors %}
                                    <div class="error-message">{{ signup_form.icon.errors[0] }}</div>
                                {% endif %}
                            </div>

                            <!-- 手机号 -->
                            <div class="mb-3">
                                {{ signup_form.phone_num.label(class="form-label") }}
                                {{ signup_form.phone_num(class="form-control") }}
                                {% if signup_form.phone_num.errors %}
                                    <div class="error-message">{{ signup_form.phone_num.errors[0] }}</div>
                                {% endif %}
                            </div>

                            <!-- 验证码 -->
                            <div class="mb-3">
                                {{ signup_form.captcha.label(class="form-label") }}
                                <div class="d-flex align-items-center">
                                    {{ signup_form.captcha(class="form-control", style="width: auto; flex: 1; margin-right: 10px;") }}
                                    <img id="cap"
                                         src="{{ url_for('user.captcha') }}"
                                         alt="验证码"
                                         title="看不清？点击刷新"
                                         class="captcha-img">
                                </div>
                                {% if signup_form.captcha.errors %}
                                    <div class="error-message">{{ signup_form.captcha.errors[0] }}</div>
                                {% endif %}
                            </div>

                            <!-- 提交按钮 -->
                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary btn-lg">立即注册</button>
                            </div>
                        </form>
                    </div>
                    <div class="card-footer text-center text-muted small">
                        已有账号？<a href="{{ url_for('user.login') }}">登录</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- jQuery (推荐使用官方 CDN) -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <!-- Bootstrap JS (已通过 bundle 引入，此处可省略，但保留也无妨) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#cap').on('click', function () {
                $(this).attr('src', "{{ url_for('user.captcha') }}?ran=" + Math.random());
            });
        });
    </script>
</body>
</html>
```